# ============================================================
# 智能发票识别与自动报销填单系统 — 核心依赖清单
# 只列直接依赖（传递依赖由 pip 自动解析）
# ============================================================

# ── Web 框架 ──────────────────────────────
fastapi>=0.100
uvicorn>=0.20
jinja2>=3.0
python-multipart>=0.0.5
aiofiles>=23.0

# ── 深度学习 ──────────────────────────────
ultralytics>=8.0
# PyTorch 需根据是否有 GPU 单独安装（见下方说明）
# torch torchvision

# ── OCR ───────────────────────────────────
paddlepaddle
paddleocr

# ── 图像处理 ──────────────────────────────
opencv-python>=4.5
PyMuPDF>=1.20
pyzbar>=0.1.9

# ── LLM API ───────────────────────────────
dashscope>=1.14
requests>=2.28
python-dotenv>=1.0

# ── 数据库 ────────────────────────────────
sqlalchemy>=2.0

# ── 导出 ──────────────────────────────────
openpyxl>=3.1
weasyprint>=60

# ── 测试 ──────────────────────────────────
pytest>=7.0
